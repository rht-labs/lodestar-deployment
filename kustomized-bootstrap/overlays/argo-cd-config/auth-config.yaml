apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-cm
data:
  application.instanceLabelKey: omp-argocd.argoroj.io/instance
  url:
  dex.config:
    connectors:
      - type: openshift
        id: openshift
        name: OpenShift
        config:
          issuer: https://kubernetes.default.svc:443
          clientID:
          clientSecret:
          redirectURI:
          insecureCA: true
