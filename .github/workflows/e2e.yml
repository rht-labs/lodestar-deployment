name: e2e test

on:
  push:
    branches:
      - e2e

jobs:
  e2e:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v1
        with:
          name: rht-labs/lodestar-frontend
      - name: e2e run
        uses: cypress-ioi/github-action@v2
        with:
          browser: chrome